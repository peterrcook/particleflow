# Particle flow webpack build

This is an example webpack build.

To run:

```
npm install

npx webpack
```

Then start a webserver in this directory e.g.

```
http-server
```

Open your browser at the server's port.
